IF (EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/../../common)
	SET(_EXCLUDE "")
ELSE()
	SET(_EXCLUDE PATTERN gdrt EXCLUDE)
ENDIF()

INSTALL(
	DIRECTORY .
	DESTINATION ${SC3_PACKAGE_SHARE_DIR}/templates/seedlink
	PATTERN ".svn" EXCLUDE
	PATTERN "CMakeLists.txt" EXCLUDE
	${_EXCLUDE}
)

